# --- Supabase (required) ---
# Supabase Dashboard → Project Settings → Data API (or API)
# "Project URL"
NEXT_PUBLIC_SUPABASE_URL=https://ugtzwnjzhdzsytdqqxhk.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_h_h3Wy4uVvoVEt88kF91dg_-kubK7hh
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InVndHp3bmp6aGR6c3l0ZHFxeGhrIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODQwNjMyNDcsImV4cCI6MjA5OTYzOTI0N30.zVY1-omIWqLucReyHVCuOV4406t6qKPv5PG8RURHY4I

# --- Supabase service role (required to store form submissions) ---
# Supabase Dashboard → Project Settings → API → "service_role" secret.
# SERVER ONLY — bypasses Row Level Security, so it must never be exposed to the
# browser (note there is no NEXT_PUBLIC_ prefix) or committed to git. The public
# concierge form has no signed-in user, so this is how it writes leads without
# granting the public anon key any write access.
SUPABASE_SERVICE_ROLE_KEY=

# --- Site URL (optional but recommended) ---
# Used as a fallback for email redirect links when the request origin is
# unavailable. In production set this to your deployed URL.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# --- Concierge transactional email (Resend) ---
# Create a key at https://resend.com/api-keys. Without it, the /api/concierge
# route accepts the request but skips the confirmation email (dev-friendly).
RESEND_API_KEY=
# Verified sender. Until you verify a domain in Resend you can use the sandbox
# sender "onboarding@resend.dev" — BUT it only delivers to your own Resend
# account email, so leads will NOT reach info@weefly.africa until the domain is
# verified at https://resend.com/domains. Then switch to:
#   CONCIERGE_FROM_EMAIL="WeeFly Concierge <concierge@weefly.africa>"
CONCIERGE_FROM_EMAIL="WeeFly Concierge <onboarding@resend.dev>"
# Optional: overrides who receives each new online request. Comma-separated.
# Defaults to info@weefly.africa,info@weefly.cv when left empty.
CONCIERGE_TEAM_EMAIL=info@weefly.africa,info@weefly.cv

# --- Concierge Chatbot · NLP (Anthropic / Claude) ---
# Powers /api/chat/parse — turns free-text messages into a structured flight
# query. Create a key at https://console.anthropic.com/settings/keys.
# Without it, the /newhome chat replies asking for the details it can't parse.
ANTHROPIC_API_KEY=
# Optional: model used for parsing. Defaults to claude-haiku-4-5 (fast + cheap,
# ideal for short extraction). Bump to claude-sonnet-5 if IATA/date resolution
# on less common routes needs more reasoning.
CONCIERGE_NLP_MODEL=claude-haiku-4-5

# --- Concierge Chatbot · Flight search (Amadeus) ---
# Powers /api/flights/search (Flight Offers Search API). Create Self-Service
# credentials at https://developers.amadeus.com. Without them, the search route
# returns realistic MOCK offers so the /newhome flow stays fully demoable.
AMADEUS_CLIENT_ID=
AMADEUS_CLIENT_SECRET=
# Test host by default; switch to https://api.amadeus.com for production keys.
AMADEUS_API_BASE=https://test.api.amadeus.com
# Currency for returned fares (ISO 4217). Defaults to EUR.
AMADEUS_CURRENCY=EUR

# --- WeePay payment gateway (Link 3) ---
# WeePay is a separate FastAPI service (see WeePay_Board_Developer_Manual_v1.docx).
# Until WEEPAY_API_URL is set, the payment stage still works: the admin records
# the agreed fare and marks it paid by hand. Setting this switches on the
# automatic payment link via POST /api/v1/payments/initiate.
WEEPAY_API_URL=
WEEPAY_API_KEY=
